<%= form_with(model: library) do |form| %>
  <% if library.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(library.errors.count, "error") %> prohibited this library from being saved:</h2>

      <ul>
        <% library.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :game_name, style: "display: block" %>
    <%= form.text_field :game_name %>
  </div>

  <div>
    <%= form.label :game_desc, style: "display: block" %>
    <%= form.text_field :game_desc %>
  </div>

  <div>
    <%= form.label :gamer_id, style: "display: block" %>
    <%= form.text_field :gamer_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
